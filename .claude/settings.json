{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(python *)",
            "shell": "bash",
            "command": "jq -c '(.tool_input.command // \"\" | tostring | ltrimstr(\" \")) as $cmd | if (($cmd | test(\"^python([.]exe)?([[:space:]]|$)\")) and (($cmd | test(\"^([.]/)?[.]venv/Scripts/python[.]exe([[:space:]]|$)\")) | not)) then {hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"Use .venv/Scripts/python.exe for this project (Python 3.10).\"}} else empty end'",
            "timeout": 5,
            "statusMessage": "Checking project Python command"
          }
        ]
      }
    ]
  }
}
